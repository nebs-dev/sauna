export class PathRecorder {
  // Method to record the path as characters
  public recordPath(path: string[]): string {
    // Convert the array of characters to a string
    return path.join("");
  }
}
